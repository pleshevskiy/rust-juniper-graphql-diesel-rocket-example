diff --git a/src/db/schema.rs b/src/db/schema.rs
index 858af4a..0cd1d61 100644
--- a/src/db/schema.rs
+++ b/src/db/schema.rs
@@ -20,10 +20,10 @@ table! {
         name -> Varchar,
         /// The `appears_in` column of the `heroes` table.
         ///
-        /// Its SQL type is `Array<Episode_enum>`.
+        /// Its SQL type is `Array<EpisodeMapping>`.
         ///
         /// (Automatically generated by Diesel.)
-        appears_in -> Array<Episode_enum>,
+        appears_in -> Array<EpisodeMapping>,
         /// The `home_planet` column of the `heroes` table.
         ///
         /// Its SQL type is `Varchar`.
